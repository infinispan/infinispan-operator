name: Automated Backporting

on:
  pull_request_target:
    types:
      - closed
    branches:
      - main

permissions:
  contents: read

jobs:
  create_backport:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - if: contains(github.event.pull_request.labels.*.name, '2.3.x')
        name: 2.3.x Backport
        uses: kiegroup/git-backporting@17bab14ff4b362d044e12c18e491b4ef8e69a4b1 # v4.10.0
        with:
          target-branch: 2.3.x
          pull-request: ${{ github.event.pull_request.url }}
          auth: ${{ secrets.INFINISPAN_RELEASE_TOKEN }}
          no-squash: true

      - if: contains(github.event.pull_request.labels.*.name, '2.4.x')
        name: 2.4.x Backport
        uses: kiegroup/git-backporting@17bab14ff4b362d044e12c18e491b4ef8e69a4b1 # v4.10.0
        with:
          target-branch: 2.4.x
          pull-request: ${{ github.event.pull_request.url }}
          auth: ${{ secrets.INFINISPAN_RELEASE_TOKEN }}
          no-squash: true

      - if: contains(github.event.pull_request.labels.*.name, 'stable')
        name: Stable Backport
        uses: kiegroup/git-backporting@17bab14ff4b362d044e12c18e491b4ef8e69a4b1 # v4.10.0
        with:
          target-branch: stable
          pull-request: ${{ github.event.pull_request.url }}
          auth: ${{ secrets.INFINISPAN_RELEASE_TOKEN }}
          no-squash: true
