schema_version: 1

name: builder-infinispan-operator
version: latest

execute:
  - script: install

artifacts:
  - name: operator.tar.gz
    url: https://github.com/infinispan/infinispan-operator/tarball/main

packages:
    manager: yum
    install:
      - golang
