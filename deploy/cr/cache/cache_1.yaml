apiVersion: infinispan.org/v2alpha1
kind: Cache
metadata:
  name: example-cache
spec:
  adminAuth:
    username:
      key: username
      name: basic-auth
    password:
      key: password
      name: basic-auth
  clusterName: example-infinispan
  name: mycache
