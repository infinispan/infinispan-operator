[id='ref_tls_cert_secret-{context}']
= Certificate Secrets

[source,options="nowrap",subs=attributes+]
----
include::yaml_annotated/secret_tls_cert.yaml[]
----

<1> adds a base64 encoded TLS key.
<2> adds a base64 encoded TLS certificate.
