{{with $v := printf "%d.%d" .Infinispan.Version.Major .Infinispan.Version.Minor}}<infinispan
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="urn:infinispan:config:{{$v}} https://infinispan.org/schemas/infinispan-config-{{$v}}.xsd
                        urn:infinispan:server:{{$v}} https://infinispan.org/schemas/infinispan-server-{{$v}}.xsd"
    xmlns="urn:infinispan:config:{{$v}}"
    xmlns:server="urn:infinispan:server:{{$v}}">{{end}}